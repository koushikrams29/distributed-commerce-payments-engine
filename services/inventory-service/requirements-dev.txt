-r requirements.txt

pytest
pytest-asyncio
httpx
# Upper bound: 4.x moved the Postgres container to testcontainers.community,
# so a major bump would break tests/conftest.py imports.
testcontainers[postgres]>=4.15,<5
